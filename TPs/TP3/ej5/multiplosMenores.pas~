Program multiplesMenores;

{En base aun K y Q ingresado por el usuario, mostrar todos los multiplos K menores que Q}

var
	K,Q,i: byte;
begin
	write('Ingrese el numero del cual desea obtener sus multiplos: ');readln(K);
	write('Ingrese el maximo del cual desea obtener multiplos: ');readln(Q);

	for i:=1 to Q do
		begin
			if ((i*K)<Q) then
				writeln('multiplo: 'i*K);
		end; 
	readln();
end;
